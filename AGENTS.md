This project uses Bun.

- Use `bun` / `bun run` / `bunx` for dependency management and scripts.
- Treat `bun.lock` as the source-of-truth lockfile.
- Do not introduce `npm`, `pnpm`, or `yarn` lockfiles unless explicitly requested.
